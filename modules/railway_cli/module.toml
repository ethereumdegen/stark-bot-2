[module]
name = "railway_cli"
version = "1.0.0"
author = "starkbot"
description = "Railway CLI — deploy and manage Railway projects from the command line"

[service]
default_port = 0

[skill]
content_file = "skill.md"

[install]
script = "install.sh"
uninstall_script = "uninstall.sh"
